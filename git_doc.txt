press 'q' to exit git log

git status

git add *
git commit -m"msg"
git push origin <branch name>

git branch
git branch <branch name>

-- Switch branch
git checkout <branch>


-- Merge to master
git checkout master
git merge <from branch>
git pull
git push origin master

— Remove:
git rm -r <folder name> | git rm <filename>
git commit -m’<msg>’
git push origin <branch>
